version: "3"

services:
  parity:
    image: parity/parity:v2.4.5
    restart: unless-stopped
    ports:
      - "30303:30303"
      - "30303:30303/udp"
      - "8545:8545"
      - "8546:8546"
    volumes:
      - ${PWD}/data:/home/parity/.local/share/io.parity.ethereum
    stop_signal: SIGINT
    stop_grace_period: 2m
    command:
      - --no-config
      - --ws-origins=*
      - --tracing=on
      - --auto-update=none
      - --max-peers=100
      - --jsonrpc-interface=all
      - --jsonrpc-apis=safe
      - --jsonrpc-hosts=all
      - --jsonrpc-threads=8
      - --jsonrpc-cors=all
      - --jsonrpc-server-threads=8
      - --jsonrpc-max-payload=10
    logging:
      driver: "json-file"
      options:
        max-size: "200k"
        max-file: "10"
